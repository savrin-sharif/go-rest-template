version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: go-rest-template:local
    ports:
      - "8080:8080"
    environment:
      APP_LOG_LEVEL: debug
      APP_SERVER_PORT: 8080
    command: ["/app/server"]
